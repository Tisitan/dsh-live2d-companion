# 修复落地报告（2026-08-31）

基线：HEAD=`44a1db4` + 工作树未提交批（多窗/多槽/皮肤）之上实施。依据 docs/code-review-2026-08-30.md 的分级裁决。未 git commit；遵循既有代码风格。

## 任务A：WordVoyage 整体删除（裁决：不注册了，整体删除）

**归档**：`_archive/wordvoyage-2026-08-31.zip`（47,183 bytes，9 条目完整核验后删除源文件）。
注意：zip 内 games/wordvoyage 的条目无前缀（`wordvoyage/engine.mjs` 形态，Compress-Archive 按 -Path 各自取根）；恢复时需手动归位至 `games/`。

**删除**：`games/wordvoyage/`（engine/ai/index.mjs + 3 测试）、`public/src/games/wordvoyage.js`、`public/src/games/wordvoyage-save.js`、`public/wordvoyage-words.json`。

**引用清理**（逐点）：
- `pet/main.js:148-154`：CARD_SIZES 删 wordvoyage 条目与注释行
- `pet/preload.js:12`：注释改 gomoku/chess
- `standalone/main.cjs:11-18`：CARD_SIZES 同款
- `public/src/game.js`：删 l2d-skin-wordvoyage CSS 2 条、GAME_PROFILES.wordvoyage 档、hideConfig 机制整体（字段+syncConfigVisibility+CONFIG_CONTROLS+2 调用点，该机制纯为词宝 difficulty 隐藏而生）、classList 三皮列表回两皮、canvasSize 注释去词宝、consumePendingRestore + `__l2dPendingRestore` 调用点整体删除（唯一写入者已删，通道成死代码）
- `public/game-card.html:20-21`：注释去词宝（改为非正方画布通用表述）
- `games/registry.mjs`：契约注释删 supportsRestore/retryHint/多标签协议——恰好还原至 HEAD 原样（git diff 归零）
- `index.js`：banterOf 正则回退 `<move>` 单标签（删 <guess>/<hint> 泛化）、retryHint 回退固定诊断文案（45s 超时与 90s askAgent 属独立改进，保留）、/game/new 的 restore 分支整体删除（startText 词宝文案「谜航继续/共享生命」随除）
- `standalone/server.cjs:847-864`：restore 分支同款删除（回退 HEAD 形态）

**零残留验证**：全项目（排 node_modules/.git/docs/_archive）grep `wordvoyage`、`supportsRestore`、`__l2dPendingRestore`、`consumePendingRestore`、`retryHint` 均零命中。

## 任务B：C1 壳活页死修复（报告方案 A'）

- `public/boot.js:1`：页面存活标记 `window.__L2D_PAGE_LIVE = true`（探针探「页面自身标记」而非渲染器存活——about:blank 型静默重建必被识破）
- `pet/main.js:344-362` `probePage()`：executeJavaScript 探标记 + Promise.race 4s 超时；2 miss→复用 reloadPage 退避、5 miss（~40s）→quit；恢复时日志
- `pet/main.js:363-366`：app 级 `child-process-gone` 取证日志 + Renderer 类型立即探活（覆盖 webContents 级事件漏派的快速通道）
- `pet/main.js:322-327`：render-process-gone/unresponsive 处理器补 reason/exitCode 取证日志（本次事故最大排查障碍即该链静默）
- 兼容性（报告论证落地）：did-finish-load 时 pageMisses 清零（`pet/main.js:317`）与 loadRetries 同钩；quit 终态走 will-quit 属主校验删凭据（:53-59 未动），由任务C 的 respawn 接管复活；与健康自尽链正交共存于同一 tick

## 任务C：M3 宿主侧桌宠意外退场重拉

- `index.js:56-58`：模块级 `petRespawnTimer` + `petRespawnHistory`（1h 滑窗）
- `index.js:1357-1378` `spawnPet()`：effect 首拉与重拉共用 spawn 主路径；exit 处理器以 `managedPetPid === child.pid` 区分意外死亡/卸载处置（卸载路径 schedulePetKill 已先置 null，exit 判 false 不重拉——卸载语义绝不诈尸）
- `index.js:1383-1406` `schedulePetRespawn()`：30s 退避（复用节流窗口）→ findLivePet 凭据探活（仍活=收养不 spawn，与 pet-lifecycle 兼容）→ lastPetSpawnAt 30s 节流 → 滑窗 3/h 上限（撞顶 warn 放弃，防崩溃循环）→ spawnPet()
- `index.js:1461` effect spawn 段收敛为 `spawnPet()` 调用；`:1465` dispose 清 respawn timer

## 任务D：M2 卡窗崩溃监听（双宿主同改）

- `pet/main.js:224` entry 加 `loadRetries`；`:238-241` render-process-gone→日志+close()（closed 钩子自清表+pushCardArea 收缩死区，🎮 可重开）；`:243-250` did-fail-load isMainFrame 过滤 + 2s/4s 两次有限退避、仍败 close()；did-finish-load 时 loadRetries 清零；cardUrl 提取复用（`:260-261`）
- `standalone/main.cjs:261,271-287`：同款（gameUrl.href 复用既有声明）

## 任务E：Minor 捎带

- ① fetch 5s 超时 `AbortSignal.timeout(5000)` + setInterval 改自递归 healthTick（`pet/main.js:368-381`）——上一轮未决不叠并发，慢网络下 failures/pageMisses 计数不失真
- ② 主窗 did-fail-load 过滤 isMainFrame（`pet/main.js:314-319`）——子 frame 失败不再误触发整页退避
- ③ `interact.js:194-200` setCardAreas 补 y/height 有限校验（畸形矩形不再静默变 NaN 丢死区）
- ④ `index.js:904,914-915,1002` 开局闸由全局布尔升级为 per-slot Set（`newGameInFlight: Set<gameId>`；原全局闸已存在——审查报告 m5 对该闸的存在误判为缺失，此处更正），gameId 提取前移，多窗异游开局互不阻塞

## 验证（任务F）

语法：`node --check` 全部改动文件通过（pet/main.js、index.js、boot.js、standalone/main.cjs、standalone/server.cjs、public/src/game.js、interact.js；registry.mjs 经动态 import 验证）。

测试全绿：
| 测试 | 结果 |
|---|---|
| `node --test pet-lifecycle.test.mjs` | 20 通过 / 0 失败 |
| `node --test games/chess/engine.test.mjs` | pass |
| `node --test games/chess/ai.test.mjs` | pass |
| `node --test games/gomoku/engine.test.mjs` | pass |
| `node games/llm-duel.test.mjs` | 17 passed / 0 failed |
| `node standalone/test.cjs` | ok（exit=0，含 cardWins/CARD_SIZES/normalizeGameId/cardEntryBySender 等卫星窗结构断言——任务D 改动不破既有断言） |

## 遗留风险

1. **standalone/main.cjs overlay 主窗仍无页面探针**（:144 仅 gone/unresponsive→quit，无退避链）：裁决范围只含 pet/main.js。独立版壳活页死风险面与修复前的桌宠版相同，挂账待裁决。
2. **C1 探针未做真机崩溃实测**（需真实渲染器崩溃场景验证 miss→reload→quit 链路）：L2D_DEBUG=1 + pet/cdp-probe.mjs 取证手段待命，下次崩溃即可坐实 08-30 报告的候选 A（launch 期静默重建）。
3. **respawn 滑窗撞顶后**（1h 内第 4 次死亡）桌宠需 remount/宿主重启恢复，warn 日志可见——这是刻意的崩溃循环熔断。
4. 审查报告 m6（前端 60s abort 与服务端 90s+45s 解说过长的观感失配）未在裁决范围内，未动。
5. 重复债 d1（card-windows 共享工厂）/d2（对局管线分层）/d3（正则防漂移断言）为挂账项，本批未实施。
