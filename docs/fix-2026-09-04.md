# 修复落地报告（2026-09-04）

基线：HEAD + 工作树未提交批（两批修复累计 25 处编辑）之上实施。依据 docs/code-review-2026-09-04.md（四批审查流水线：批1 宿主后端 / 批2 前端内核 / 批3 standalone 独立运行时 / 批4 pet 壳+games 内核）的分级裁决。未 git commit；遵循既有代码风格；除声明的两处偏离外未顺手重构。行号为各批施工时点的工作树行号（批 2 在 index.js/server.cjs 有插入，批 1 引用行号随之平移）。

## 修复批 1（P0 止血六条）

### P0-1 standalone 卫星窗死区推送整链断裂（审查报告批3 🔴R1）
- 位置：`standalone/main.cjs:222-236`、`:261`
- 改动：`pushCardArea`/`l2d-game-bounds` 引用作用域外 `cardWin`（每次 IPC 必 ReferenceError，`l2d-game-area` 一帧都发不出）→ 整体替换为 `pet/main.js:163-185` 的多窗版（遍历 `cardWins` 收集矩形，1 窗单矩形/多窗数组/0 窗 null 兼容语义逐字对齐）；卡窗创建补 `roundedCorners: false` 对齐 pet/main.js:215 的 DPI 治疗链。
- 验收：`node --check` exit 0。

### P0-2 game.js 走子失败路径跨槽污染（审查报告批2 🔴R1）
- 位置：`public/src/game.js:554`、`:572-581`
- 改动：请求发起处捕获 `const reqGame = selectedGame` 槽位快照；catch 内守卫 `if (reqGame !== selectedGame) { fx = null }` 才不回滚/不改 `state.busy`/不插 `commentary`——等待期间切游戏后，旧槽的超时失败不再污染新对局（对照 server.cjs 同函数已有的代际守卫补齐前端侧）。
- **偏离声明（质检已裁决采纳）**：施工单字面为「catch 内 `if (reqGame !== selectedGame) return`」，实际落地为守卫只包 state 变异三行、清尾照旧。理由：字面 return 会跳过 `fx = null`（旧动画帧残留，新局 render 会拿旧坐标画错）与 `clearTimeout(timeout)`/`moveInFlight = false` 收尾（后者虽有 chip 路径复位兜底，但多一层锁死风险面）。守卫语义与施工单意图（失败不碰新局）一致。
- 验收：`node --check` exit 0。

### P0-3 穿透白名单漏 `#l2d-game-menu`（审查报告批2 🔴R2）
- 位置：`public/src/interact.js:28`
- 改动：`uiHit()` 的 closest 选择器在 `#l2d-pet-menu` 后补 `#l2d-game-menu`——桌宠穿透态下 🎮 菜单弹层悬在模型包围盒外时不再点击穿透到桌面。
- 验收：`node --check` exit 0。

### P0-4 宿主静态读流无 error 监听（审查报告批1 静态路由项）
- 位置：`index.js:1348`（施工时 :1338，批 2 平移）
- 改动：`createReadStream(file).pipe(res)` → `createReadStream(file).on('error', () => res.destroy()).pipe(res)`——读流失败（文件被删/权限）不再逃逸成宿主进程级未捕获异常。
- 验收：`node --check` exit 0。

### P0-5 发布清单补漏
- 位置：根 `package.json` files
- 改动：补 `"standalone/preload-card.cjs"`（卡窗桥缺失会导致独立版发布包启动即崩）与 `"standalone/README.md"`。
- 验收：`JSON.parse` 通过。

### M1 AgentPreset 名称取值层级
- 位置：`index.js:899`（施工时 :883）
- 改动：`/live2d/game/presets` 的 `p.metadata?.name ?? p.id` → `p.name ?? p.id`（AgentPreset 顶层 name，已核实宿主 d.ts）。
- 验收：`node --check` exit 0。

## 修复批 2（P1 协议分叉 + 安全加固 + 死代码清扫）

### P1-1 standalone /import 补 409 exists/overwrite 流（审查报告批3 🟡Y1）
- 位置：`standalone/server.cjs:1013-1017`
- 改动：`isInside` 校验后、写盘前插入 `overwrite !== '1' && fs.existsSync(target)` → `409 {error: 文件已存在…（确认覆盖请加 overwrite=1）}`；参数名/文案逐字对齐宿主 index.js 同路由。panel.js 的 409→confirm→带 overwrite=1 重传确认 UX 在独立版即刻生效。
- 验收：`node --check` exit 0 + standalone smoke ok。

### P1-2 standalone /model POST 补 delete 分支（审查报告批3 🟡Y2）
- 位置：`standalone/server.cjs:961-984`
- 改动：`{delete: dir}` → `safeSegment(dir,128)` → 404 existsSync → 400 `readdirSync.some(f => f.endsWith('.model3.json'))`（大小写处理照抄宿主 :633，无 toLowerCase）→ `fsp.rm(recursive,force)`；删的若是当前模型（`modelPath.startsWith(dir + '/')`）则重扫回落 `models[0].path` + 写 selectionFile + broadcast，响应 `{deleted, reset}` 与宿主同构。前端删除按钮不再落入 404 "model not found" 死端。
- 验收：`node --check` exit 0 + standalone smoke ok。

### P1-4 台词编辑器独立版入口隐藏（半条执行、半条停做）
- 位置：`public/src/panel.js:555`
- 改动：⚙ 菜单注入处加 `menu.querySelector('[data-act="quips"]').hidden = STANDALONE`——独立服无 /quips、/quips-config 路由，编辑器全部写操作必 404，入口按 `profileTab.hidden = !STANDALONE`（:1074）同风格收起。
- **停做声明（质检已裁决通过）**：施工单同条的「绑定编辑器保存入口隐藏」未实施——其前提「独立版缺 /profile」为批 2 的误报，批 3 已修正（server.cjs:997 存在完整 /profile 路由）；独立版绑定保存真实可用，隐藏等于砍功能。审查报告 docs/code-review-2026-09-04.md 批3 节有该自我纠错记录。

### C4 /live2d/import 加固（审查报告批1 安全项）
- 位置：`index.js:1141-1146`（扩展名白名单）、`:368-376`（超限断流）
- 改动：① 路径校验后、读 body 前加白名单 `['json','moc3','png','jpg','jpeg','webp']`（json 覆盖 model3/exp3/motion3/physics3/userdata3/pose3/cdi3/profile 族；webp 为 Cubism 4.2+ 官方纹理；无后缀/html/js/svg/mjs/wasm 一律 400）——静态路由按原后缀回吐，可执行/可渲染后缀不得入库；② readRawBody 首次超限即 `reject + req.destroy()`，不再收完全量（destroy 后的 aborted error 落在已 settle 的 promise 上为 no-op）。readJsonBody 未动。
- 验收：`node --check` exit 0。

### M2 setState 同值短路（审查报告批1 M2 轻量版）
- 位置：`index.js:428`
- 改动：函数首行 `if (perSession.get(id)?.state === state) return`——working/thinking 被逐 token 事件（assistant/chunk 等）反复打时不再重复 aggregate+publish。done/error 连发时保留首个 hold 倒计时（语义差异已评估：倒计时不顺延，安全）。
- 验收：`node --check` exit 0。

### M3 SSE 生命周期 + hold/reap 定时器卸载收尾（审查报告批1 M3）
- 位置：`index.js:490-513`
- 改动：state-stream effect 改显式 disposer：先调 register 返回的 `dispose()`，再 `for (const res of sseClients) res.end()` + `sseClients.clear()`，最后遍历 `perSession` 清 `entry.timer`（done/error hold 与 5 分钟 reap 均为 setTimeout，用 clearTimeout）——插件重挂载后旧定时器不再 publish 到已关闭的流。
- 验收：`node --check` exit 0。

### P2-6 死代码清扫（审查报告批4 🟡Y2/🟢）
- `pet/preload.js`：删 `onGameFocus`（`l2d-game-focus` 全仓无发送方无监听方）。
- `public/game-card.js`：删悬停武装段（两主进程 `l2d-game-focusable` 均为 noop，编排已死）；保留 `const card`（拖动把手仍引用）。
- `pet/preload-card.js` + `standalone/preload-card.cjs`：删 `setFocusable` 桥接项，两处失真注释（「运行期焦点能力切换」「焦点能力」）随行更正。
- `standalone/main.cjs:221`：卡窗注释「五子棋/国象/词宝谜航三个游戏」→「五子棋/国象两个游戏」（词宝 08-31 已归档）。
- `games/chess/index.mjs`：删与 registry `defaultQuipKey` 逐字相同的冗余 `pickQuipKey`（消费端 `g.def.pickQuipKey ?? defaultQuipKey` 自动回落，llm-duel.test 17 断言回归通过）。
- 验收：四文件 grep `setFocusable|onGameFocus` 零残留。

### P2-5 发布清单瘦身（审查报告批4 🟡Y3）
- 位置：根 `package.json` files
- 改动：移出 `pet/cdp-probe.mjs`、`pet/restart-dsh-web.ps1`（含硬编码本机绝对路径 + 强杀 3080 端口进程，不该随包发布）、`pet/standalone-server.mjs`（三者留仓库为开发工具，README 有文档引用，非孤儿）；`"games/gomoku"` 整目录改枚举 `engine.mjs`/`ai.mjs`/`index.mjs`，测试文件不入包，与 chess 对称。
- 验收：`JSON.parse` 通过。

## 验证（两批合计）

语法：`node --check` 全部改动文件通过——`standalone/main.cjs`、`standalone/server.cjs`、`standalone/preload-card.cjs`、`pet/preload.js`、`pet/preload-card.js`、`games/chess/index.mjs` 走文件直检；`index.js`、`public/src/game.js`、`public/src/interact.js`、`public/src/panel.js`、`public/game-card.js` 走 `--input-type=module` stdin；`package.json` 走 JSON.parse。全部 exit 0。

测试全绿（两轮各跑一遍，结果一致）：
| 测试 | 结果 |
|---|---|
| `node pet-lifecycle.test.mjs` | 20 通过 / 0 失败 |
| `node games/chess/engine.test.mjs` | 24 passed / 0 failed |
| `node games/chess/ai.test.mjs` | 8 passed / 0 failed |
| `node games/gomoku/engine.test.mjs` | 27 passed / 0 failed |
| `node games/llm-duel.test.mjs` | 17 passed / 0 failed |
| `node standalone/test.cjs` | ok（exit=0，含 import+选模用例，P1-1/P1-2 路由回归通过） |

## 生效条件（本批未 git commit）

- **需重启进程生效**：`index.js`（宿主插件：重启 dsh web / 重挂载 live2d-companion）、`standalone/server.cjs`、`standalone/main.cjs`、`standalone/preload-card.cjs`、`pet/preload.js`、`pet/preload-card.js`（Electron 壳：重启桌宠/独立版进程；壳层 preload 改动必须整窗重启）。
- **按请求现读即刻生效**：`public/` 下静态文件（`src/game.js`、`src/interact.js`、`src/panel.js`、`game-card.js`）——刷新挂件页/重载桌宠页面即生效，无需重启任何进程。
- **下次打包生效**：根 `package.json` files 白名单（P0-5/P2-5）在下次 `npm publish`/打包时体现。

## 刻意未做项（挂账，均为审查报告在案条目）

1. **display mode bundle 层修复**：涉及宿主 cordis.patch.yml 文本手术层（rewriteLive2dConfig）与 mount 生命周期语义，风险面大于本批收益，待独立裁决（审查报告批1 对应条目）。
2. **panel.js 拆分**：2018 行上帝文件按批 2 报告的六模块边界拆分属纯结构重构，与行为修复混入同一 diff 会污染审查，单独立项（审查报告批2 🟡Y1，含逐区间拆分表与四件套去重清单）。
3. **模型素材去重 27.4MB**：`public/model/nori/` 下 `texture_00_corrupt.png` 与 4096 贴图冗余属资产运维（涉及 git 历史与资产替换流程），非代码修复范畴（审查报告批1 资产节）。
4. **M5 收养竞态**：pet-lifecycle 凭据探活与宿主 spawn 的窗口期竞态需跨进程时序协议改动与真机验证，本批只做与两形态行为一致性直接相关的 P1（审查报告批1 M5）。
5. **M6 凭据原子写**：pet.pid 临时文件+rename 原子化需与宿主 stale 探活自愈路径联动验证（审查报告批1 M6）。
